[package]
name = "telemetry_analyzer"
version = "0.1.0"
edition = "2026"
target = "cpp"
entry = "src/main.nyx"
description = "Package and foreign binding consumer example"

[dependencies]
stats_core = { path = "packages/stats_core", version = "1.0.0" }
