# Nyx Package Manifest
[package]
name = "nyx"
version = "4.0.0-rc.2"
edition = "2026"
authors = ["Nyx Authors"]
description = "Systems-oriented programming language with multi-target compilation"
license = "Apache-2.0"

[dependencies]
std = "4.0.0"

[build]
target = "cpp"
output_type = "exe"
opt_level = 2
