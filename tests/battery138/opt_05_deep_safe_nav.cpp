// Auto-generated by nyx Systems Compiler (hecpp - C++20)
#include <iostream>
#include <string>
#ifdef _WIN32
#include <windows.h>
#endif
using namespace std;

// --- nyx Standard Core Helpers ---
template<typename F> struct _NyxScopeExit { F f; ~_NyxScopeExit() { f(); } };
template<typename F> _NyxScopeExit<F> _nyx_make_scope_exit(F f) { return {f}; }

struct City {
    string name;
    City() : name() {}
    City(string name) : name(name) {}

};

struct Address {
    optional<City> city;
    Address() : city() {}
    Address(optional<City> city) : city(city) {}

};

struct Profile {
    optional<Address> addr;
    Profile() : addr() {}
    Profile(optional<Address> addr) : addr(addr) {}

};

int main() {
#ifdef _WIN32
    SetConsoleOutputCP(65001);
    SetConsoleCP(65001);
#endif
    cout << boolalpha;
    optional<Profile> p = nullptr;
    cout << (p.addr.city.name.value_or(string("No City"))) << endl;
    return 0;
}